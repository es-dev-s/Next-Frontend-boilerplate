import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      description="Term schedules, events, and campus rhythms — reserved for the scheduling module."
    />
  );
}
