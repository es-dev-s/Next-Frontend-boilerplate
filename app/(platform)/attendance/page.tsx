import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function AttendancePage() {
  return (
    <PlaceholderPage
      title="Attendance"
      description="Daily roll, exceptions, and period tracking — ready for the attendance engine."
    />
  );
}
